name: CI Migration Trigger
on:
  workflow_dispatch:
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger backend CI on migration branch
        run: echo "Backend CI is configured to validate refactor/backend-modular"
